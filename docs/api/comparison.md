# Comparing values

Whether two values from different files mean the same thing. Shared by every format, so a rule fixed once is fixed everywhere.

```{eval-rst}
.. automodule:: rs_metadata.normalize
   :members:
```

