# Running a validation

`validate` takes an `Options` and returns a `Report`. Everything else on this page describes what comes back.

```{eval-rst}
.. automodule:: rs_metadata.core
   :members: validate, Options
```

```{eval-rst}
.. automodule:: rs_metadata.scaffold
   :members: plan, write, Plan
```

